export interface HospitalBookmark {
    googleMapId: string;
    bookmarkDate: string;
}

export interface HospitalDetail {
    id: string;
    name: string;
    bookmarkDate: string;
    address: string;
    distance: string;
}
