export interface HospitalFirstData {
    googleMapId: string;
    bookmarkDate: string;
}

export interface HospitalInfo {
    id: string;
    name: string;
    bookmarkDate: string;
    address: string;
    distance: string;
}
